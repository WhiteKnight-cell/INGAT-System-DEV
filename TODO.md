- [x] Refactor `templates/admin/report_detail.html` to a single clean layout matching the wireframe (left col 7/12 + right col 5/12) with fixed sidebar and no overflowing cards.

- [ ] Remove duplicated/unbalanced HTML blocks (extra sidebar nesting, stray cards outside the main container, duplicate modals, merge-conflict marker).
- [ ] Ensure Map iframe, AI letter preview + download buttons, and Status Update + Status History timeline render only once and in correct columns.
- [ ] Add/adjust necessary CSS to enforce `main-content` bounds and prevent horizontal overflow.
- [ ] Quick local run: open the complaint detail page and visually verify (no overlapping, cards within container).

